<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Valida_Motivo_Unidade</fullName>
    <active>true</active>
    <description>Essa regra torna obrigatório o campo Motivo Unidade quando a oportunidade é encaminhada para a Unidade.</description>
    <errorConditionFormula>AND(($Permission.Bypass_Permission = False),
AND(
Unidade__c,
ISBLANK(TEXT(Motivo_Unidade__c))
))</errorConditionFormula>
    <errorDisplayField>Motivo_Unidade__c</errorDisplayField>
    <errorMessage>Sempre que o campo Unidade for selecionado, é necessário informar o motivo.</errorMessage>
</ValidationRule>
